import React from 'react';
import Navbar from '../components/Navbar';

const cart = () => {
    return (
        <div>
            <Navbar/>
            <h1>cart</h1>
        </div>
    );
};

export default cart;
